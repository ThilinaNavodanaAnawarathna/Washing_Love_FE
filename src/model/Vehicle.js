export default class Vehicle {
  constructor(id, user, brand, model, year, vehicleNumber) {
    this.id = id;
    this.user = user;
    this.brand = brand;
    this.model = model;
    this.vehicleNumber = vehicleNumber;
    this.year = year;
  }
}